This code is based on the implementation available at https://github.com/eriklindernoren/PyTorch-GAN

To run the code:

python main.py --run 0
python unit.py --dataset_name mammo --run 0
python test.py --dataset_name mammo  --run 0
rm -rf images
rm -rf data
rm -rf saved_models
